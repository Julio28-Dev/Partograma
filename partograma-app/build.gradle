plugins {
    id 'com.android.application' version '8.1.4' apply false
    // Usado (condicionalmente, ver app/build.gradle) para ler o google-services.json
    // do Firebase quando ele existir. Sem esse arquivo o plugin nao e aplicado e o
    // build continua funcionando normalmente.
    id 'com.google.gms.google-services' version '4.4.2' apply false
}
