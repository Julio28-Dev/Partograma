<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Expoe apenas a pasta de relatorios em cache (PDFs gerados por PdfRelatorioGenerator)
         via content:// URI. Necessario porque o app tem targetSdk 26 (>= 24), que proibe
         compartilhar Uri.fromFile() diretamente (FileUriExposedException). -->
    <cache-path name="relatorios" path="relatorios/" />
</paths>
